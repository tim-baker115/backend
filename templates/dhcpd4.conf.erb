include "/etc/dhcpd4-MASTER.conf";

#Static config file used for allocating set configurations to specific boxes.

#group { #<Group name>
#  filename "<filename>" ;
#  host <hostname> { hardware ethernet <MAC address> ; fixed-address <IP address> ; }
#}
